<p id="notice"><%= notice %></p>

<img src="http://i.imgur.com/6Yj3pJ0.png"/>
<h3>LilyPix Index</h3>

<%= link_to "<button>Upload</button>".html_safe, new_picture_path %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Caption</th>
      <th>Url</th>
      <th>Vote</th>
      <th>Hot</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><%= picture.title %></td>
        <td><%= picture.caption %></td>
        <td>​<%= image_tag picture.url %></td>
        <td><%= picture.vote %></td>
        <td><%= picture.hot %></td>
        <!-- <td><%= link_to 'Comment', new_picture_comment_path(picture_id: picture.id) %></td> -->
        <td><%= link_to "<button>View</button>".html_safe, picture %></td>
        <!-- <td><%= link_to 'Edit', edit_picture_path(picture) %></td> -->
        <!-- <td><%= link_to 'Destroy', picture, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<img src="http://i.imgur.com/dPWrScQ.png"/>