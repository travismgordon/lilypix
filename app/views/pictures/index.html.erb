<p id="notice"><%= notice %></p>
<div class="pix">

</div>





<%= image_tag "logo.png", class:"lily_logo" %>

<h3>Frontpage</h3>
<%= link_to "<button>Upload</button>".html_safe, new_picture_path %>

<main class="no-touch">

  <div class="wrap">
  <% @pictures.each do |picture| %>  
  
    <!-- Define all of the tiles: -->
    <div class="box">

      <div class="boxInner">

        <%= image_tag picture.url, class:"tile_image" %>

        <div class="titleBox">
          <% if picture.hot == true %>
              <img src="http://i.imgur.com/A4HGpmd.png", class="hot_image">
            <% else %>
            <% end %>
            <%= picture.title %>
            <%= link_to "<button>View</button>".html_safe, picture %>
          </div>

      </div>

    </div>
  
  <% end %>
  </div> 
</main>

<br>

<%= image_tag "sobe.png", class:"sobe_logo" %>
<footer>© 2015</footer>